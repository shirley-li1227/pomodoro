# Pomodoro Timer

一个基于 React + TypeScript + Vite 的番茄钟示例。

## 已实现功能

- 默认工作 `25` 分钟，休息 `5` 分钟
- 支持 `开始`、`暂停`、`重置`
- 倒计时显示格式为 `MM:SS`
- 倒计时结束后触发提醒（浏览器通知 + 提示音，通知不可用时回退为 `alert`）

## 启动方式

```bash
npm install
npm run dev
```